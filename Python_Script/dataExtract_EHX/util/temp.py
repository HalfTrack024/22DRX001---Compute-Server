#tmp = 'panelguid, XPos, OpText, OpCode_FS, ZPos_FS, YPos_FS, SsUpPos_FS, OpCode_MS, ZPos_MS, YPos_MS, SsUpPos_MS, ImgName, OBJ_ID, loadDate'
#print(tmp.lower())

subassembly = {
            'PanelGuid': 123,
            'SubAssemblyGuid': 456,
            'FamilyMember': 789,
            'FamilyMemberName': 321,
            'Width': 654
}

print(subassembly['PanelGuid'])